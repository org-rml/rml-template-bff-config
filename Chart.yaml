apiVersion: v2
name: rml-base-chart
description: Template chart for creating new rml BFF charts.
type: application
version: 0.1.0
appVersion: "1.0.0"
dependencies:
  - name: rml-base-chart
    version: v1.0.0
    repository: "http://localhost:8082/repository/rml-helm/"
